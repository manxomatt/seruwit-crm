/**
 * Cross-module switcher for the Finance group.
 * Disabled inside pages per user requirement (now navigated via the sidebar FINANCE menu).
 */
export default function FinanceNav(): JSX.Element | null {
    return null;
}
